// This file might still be needed for genkit dev commands,
// but it no longer needs to import flows defined elsewhere.
// Keep it empty or add other development-specific configurations if needed.

console.log("Genkit dev server helper running...");
